import styled from "styled-components";

const SBtnAndTitle = styled.div`
    position: relative;
    display: flex;
    flex-wrap: nowrap;
    flex-direction: row;
    align-items: center;
    width: calc(100%);
    > h1 {
        margin-left: 25%;
    }
`

export default SBtnAndTitle